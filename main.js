let a = prompt("Ismingizni kiriting...")
let b = prompt("Space Id kiriting...")
let c = prompt("Space password kiriting...")

let Space = {
    name: a,
    id: b,
    password: c,
}
console.log(Space);